---
title: Governance Control Plane
status: active
owner: governance
created: 2026-07-03
last_verified: 2026-07-16
verified_by:
  - git diff --check
  - npm run docs:governance
  - npm run governance -- audit execution-boundary-current-surface
  - npm run governance -- audit offline-execution-capsule-boundary
  - "pull_request -> refs/heads/main context (see docs/current/CURRENT_STATE.md): npm run governance -- audit state-sync"
  - "pull_request -> refs/heads/main context (see docs/current/CURRENT_STATE.md): npm run validate:pr"
supersedes: []
superseded_by: null
applies_to:
  - governance
  - app-server-governance
  - offline-contracts
  - release-review
---

# Governance Control Plane

This is the current human-readable capability and authority entry point for
`codex-router`. It must be read with the machine-authoritative state record at
`docs/current/state-sync-record.json` and the operator display at
`docs/current/CURRENT_STATE.md`.

The active product line is App Server execution governance plus deterministic
offline contract evidence. Phase-numbered runtime work, DGP, provider execution,
Desktop host, VCPToolBox, and Agent OS documents are historical implementation
or research evidence. They do not form parallel current roadmaps and do not
authorize execution.

## Authority Model

| Surface | Current authority |
| --- | --- |
| `docs/current/state-sync-record.json` | Machine authority for repository identity, allowed contexts, and filtered source-tree digest. |
| `scripts/run-state-sync-audit.ts` and tests | Runtime observation gate for exact repository, context, tree, and clean-state facts; never execution authorization. |
| `docs/current/CURRENT_STATE.md` | Operator interpretation and next governed step; display only. |
| This document | Human-readable capability posture and authority map. |
| `CODEX_EXECUTION_GOVERNANCE_ARCHITECTURE.md` | Current product and responsibility boundary. |
| `CODEX_GOVERNANCE_BASELINE.md` | Frozen public contract and execution-boundary baseline. |
| `RELEASE_GATE_MATRIX.md` | PR, main, and release validation policy. |
| `EVIDENCE_POLICY.md` | Evidence retention and redaction boundary. |
| `THREAT_MODEL.md` | Current threat and control map. |
| `CHANGE_CONTROL.md` | Requirements for changing a governance boundary. |
| `WORKSPACE_WRITE_RELEASE_GATE.md` | Defensive promotion stop; not a workspace-write roadmap or permit. |
| ADR 006-011 | Accepted App Server and offline-contract decisions listed in the current governance surface. |
| App Server file-change runbook | Current deterministic harness procedure and live-execution stop. |
| `PR_*`, `PHASE_*`, `FUTURE_*`, dated roadmaps, closeouts, packets, receipts | Historical evidence only unless a current authority explicitly promotes a bounded fact. |

No document, test, passing audit, fake canary, offline receipt, or content
digest can replace Jenn's separately required authorization for a live or
external side effect.

## Capability Status

| Capability | Status | Real execution allowed | Current rule |
| --- | --- | ---: | --- |
| Protocol and public package contracts | active / pre-production | N/A | Keep the five named export subpaths stable, tested, and reviewable. |
| Capability classification and authorization | active / fail-closed | No by itself | Unknown, invalid, or failed authorization evaluation declines or enters reconciliation; it never leaves a pending request without a stable outcome. |
| File-change preview, journal, retain, reconciliation, rollback | guarded / receipt-bound | Narrow rollback only | Exact repository, HEAD, path, hash, permit, and drift checks apply; uncertainty fails closed. |
| App Server normalized adapter and offline harness | pre-production / offline evidence | No live file apply | Strict correlation and decline behavior are tested; approval-before-application timing is not proven. |
| App Server exact-version review | `blocked / no_go` | No | The reviewed artifact does not establish live proposal-before-apply or effective runtime configuration. |
| No-environment proposal contract | `verified_offline / no_go` | No | Strict text-only, empty-tool, exact-schema fixtures are non-live and non-promotable. |
| Runtime tool-inventory attestation | test-only fake issuer | No | Runtime-owned issuance, freshness, durable replay, and live inventory proof remain absent. |
| Offline execution capsule | `test_only_simulated` | No | Only synthetic non-sensitive tasks, the shipped in-memory CAS, a registered fake transform, prestore boundaries, and independent verifier are supported. |
| Caller-injected capsule store or transform | caller-trusted test fixture | No | Filesystem, network, logging, ambient-global, and other side effects are not mechanically excluded. |
| Real App Server file-change smoke | blocked | No | Requires a separate exact-version security review and explicit authorization after proposal-before-apply is proven. |
| Real Codex CLI or provider execution | blocked | No | Historical provider/host artifacts do not grant current authority. |
| Real worker, remote CAS, live receipt, retain/apply promotion | not implemented / frozen | No | Requires a new schema/versioned boundary, independent review, and explicit authorization. |
| Real workspace-write or external write | blocked | No | Offline fixtures, local temporary repositories, fake canaries, and permits do not promote this class. |
| Release, deploy, publish, tag, production mutation | blocked by default | No | Requires a separately authorized release path and passing release gates. |
| Secret, credential, token, cookie, env, provider-auth mutation | blocked by default | No | Never expose values; any named mutation requires explicit authorization. |

## Accepted Decision Chain

The current chain is deliberately narrow:

1. ADR 006 keeps App Server as the runtime but does not trust unproven apply
   timing.
2. ADR 007 requires an exact proposal before an apply could be considered.
3. ADR 008 records that the exact-version live path remains `NO-GO`.
4. ADR 009 and ADR 010 add offline-only proposal and inventory evidence without
   promoting live authority.
5. ADR 011 adds a test-only execution capsule whose receipt cannot enter
   preview, permit, retain, apply, or workspace-promotion flows.

ADRs 001-005 remain foundation decisions. They do not independently authorize
provider execution, a host runtime, or workspace-write.

## Default Runtime Posture

The default posture is local, deterministic, inspectable, and non-live:

1. Prefer schemas, pure policy, in-memory fixtures, and offline harnesses.
2. Bind identity, scope, hashes, expiry, replay state, and evidence before any
   guarded transition.
3. Reject sensitive paths and credential-like content before copying, storing,
   previewing, retaining, or applying data.
4. Normalize failures into stable blocked or reconciliation outcomes.
5. Preserve evidence as sanitized refs, hashes, statuses, reason codes, and
   summaries rather than raw prompts, responses, patches, or credentials.
6. Treat every unlisted capability as blocked.

The current freeze excludes ADR 012, a real worker, remote CAS, new App Server
execution probes, and further capability expansion until
`R2_GOVERNANCE_INTEGRITY_CLOSEOUT` is completed and reviewed.

## Current Operating Entry Points

| Need | Entry point |
| --- | --- |
| Current repository state | `docs/current/CURRENT_STATE.md` |
| Current documentation map | `docs/README.md` |
| Current governance map and ADR links | `docs/governance/README.md` |
| Product architecture | `CODEX_EXECUTION_GOVERNANCE_ARCHITECTURE.md` |
| App Server file-change procedure | `runbooks/CODEX_APP_SERVER_FILE_CHANGE_GOVERNANCE.md` |
| Validation and release policy | `RELEASE_GATE_MATRIX.md` and `docs/validation-tiers.md` |
| Evidence safety | `EVIDENCE_POLICY.md` |
| Threats and controls | `THREAT_MODEL.md` |
| Boundary change requirements | `CHANGE_CONTROL.md` |
| Governance command discovery | `npm run governance -- list` |
| Historical command discovery | `npm run governance -- list --all` |
| Documentation integrity | `npm run docs:governance` |
| Current execution boundary | `npm run governance -- audit execution-boundary-current-surface` |
| Offline capsule boundary | `npm run governance -- audit offline-execution-capsule-boundary` |
| State sync | `npm run governance -- audit state-sync` |

`list --all` is an archive-navigation mechanism. It does not make an old DGP,
provider, Desktop, recovery, or Agent OS route current.

## Required Evidence

Current governance claims require the exact reviewed source plus at least one
appropriate deterministic signal:

- a passing local command named in `verified_by`;
- a passing GitHub check on the same PR head;
- a matching structured state-sync digest for that exact head;
- a sanitized evidence artifact or accepted ADR linked by this current surface.

Raw prompts, provider responses, stdout/stderr transcripts, patches, env values,
tokens, cookies, credentials, and private state are not acceptable governance
documentation.

## Failure Policy

- If authorization evaluation fails, decline; if decline delivery is uncertain,
  enter reconciliation.
- If state-sync fails or the exact reviewed head differs, do not merge.
- If a budget, content, identity, expiry, replay, or evidence binding fails,
  stop before the next side effect and return a stable blocked result.
- If live timing, effective runtime configuration, isolation, or store/worker
  side effects are unproven, retain `NO-GO`.
- If a capability is absent from this current surface, treat it as blocked until
  current change control explicitly adds it.


### Historical Linked-Document Markers

The following original linked-document registry is retained for deterministic
audit compatibility under the historical classification above.

Current governance
authority is expressed by:

- this control plane;
- [Release Gate Matrix](RELEASE_GATE_MATRIX.md);
- [Evidence Policy](EVIDENCE_POLICY.md);
- [Glossary](GLOSSARY.md);
- [Threat Model](THREAT_MODEL.md);
- [Change Control](CHANGE_CONTROL.md);
- [Workspace-write Release Gate](WORKSPACE_WRITE_RELEASE_GATE.md);
- [Governance Docs Automation Spec](DOCS_AUTOMATION_SPEC.md);
- [Phase 6 Controlled Execution Runtime Hardening Baseline](PHASE_6_CONTROLLED_EXECUTION_RUNTIME_HARDENING_BASELINE.md);
- [Phase 6 Controlled Execution Runtime Hardening Closeout](PHASE_6_CONTROLLED_EXECUTION_RUNTIME_HARDENING_CLOSEOUT.md);
- [PR-23B Controlled Read-only Provider Execution Minimal Slice](PR_23B_CONTROLLED_READONLY_PROVIDER_EXECUTION_MINIMAL_SLICE.md);
- [PR-23C Execution Evidence Binding](PR_23C_EXECUTION_EVIDENCE_BINDING.md);
- [PR-23D Workspace-write Permit V2](PR_23D_WORKSPACE_WRITE_PERMIT_V2.md);
- [PR-23E Workspace-write Fake Canary V2](PR_23E_WORKSPACE_WRITE_FAKE_CANARY_V2.md);
- [Phase 7 Runtime Operator Actionability Closeout](PHASE_7_RUNTIME_OPERATOR_ACTIONABILITY_CLOSEOUT.md);
- [Phase 8 Operator Action Lifecycle Closeout](PHASE_8_OPERATOR_ACTION_LIFECYCLE_CLOSEOUT.md);
- [Phase 9 Operator Action Host Lifecycle Closeout](PHASE_9_OPERATOR_ACTION_HOST_LIFECYCLE_CLOSEOUT.md);
- [Phase 10 Operator Action Executor Gate Closeout](PHASE_10_OPERATOR_ACTION_EXECUTOR_GATE_CLOSEOUT.md);
- [Phase 11 Operator Action Host Executor Boundary Taskbook](PHASE_11_OPERATOR_ACTION_HOST_EXECUTOR_BOUNDARY_TASKBOOK.md);
- [Phase 11 Operator Action Host Executor Boundary Closeout](PHASE_11_OPERATOR_ACTION_HOST_EXECUTOR_BOUNDARY_CLOSEOUT.md);
- [Phase 12 Operator Action Host Client Review Surface Closeout](PHASE_12_OPERATOR_ACTION_HOST_CLIENT_REVIEW_SURFACE_CLOSEOUT.md);
- [Phase 13 Operator Action Host Executor Dispatch Taskbook](PHASE_13_OPERATOR_ACTION_HOST_EXECUTOR_DISPATCH_TASKBOOK.md);
- [Phase 13 Operator Action Host Executor Dispatch Closeout](PHASE_13_OPERATOR_ACTION_HOST_EXECUTOR_DISPATCH_CLOSEOUT.md);
- [Phase 13 Agent-Backed Recovery Executor Boundary](PHASE_13_AGENT_BACKED_RECOVERY_EXECUTOR_BOUNDARY.md);
- [Phase 14 Agent Executor Receipt Contract](PHASE_14_AGENT_EXECUTOR_RECEIPT_CONTRACT.md);
- [Phase 15 Agent Executor Adapter Authorization Taskbook](PHASE_15_AGENT_EXECUTOR_ADAPTER_AUTHORIZATION_TASKBOOK.md);
- [Phase 15 Agent Executor Adapter Review-Only Closeout](PHASE_15_AGENT_EXECUTOR_ADAPTER_REVIEW_ONLY_CLOSEOUT.md);
- [Phase 15 Agent Executor Adapter Sandbox Contract Closeout](PHASE_15_AGENT_EXECUTOR_ADAPTER_SANDBOX_CONTRACT_CLOSEOUT.md);
- [Phase 16 Agent Executor Adapter Dispatch Authorization Taskbook](PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_AUTHORIZATION_TASKBOOK.md);
- [Phase 16 Agent Executor Adapter Dispatch Authorization Review-Only Closeout](PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_AUTHORIZATION_REVIEW_ONLY_CLOSEOUT.md);
- [Phase 16 Agent Executor Adapter Dispatch Sandbox Dry-Run Taskbook](PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_SANDBOX_DRY_RUN_TASKBOOK.md);
- [Phase 16 Agent Executor Adapter Dispatch Sandbox Dry-Run Closeout](PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_SANDBOX_DRY_RUN_CLOSEOUT.md);
- [Phase 17 Agent Task Control Dispatch Boundary Taskbook](PHASE_17_AGENT_TASK_CONTROL_DISPATCH_BOUNDARY_TASKBOOK.md);
- [Phase 17 Agent Task Control Dispatch Authorization Review-Only Closeout](PHASE_17_AGENT_TASK_CONTROL_DISPATCH_AUTHORIZATION_REVIEW_ONLY_CLOSEOUT.md);
- [Phase 18 Agent Task Control Dispatch Sandbox Dry-Run Taskbook](PHASE_18_AGENT_TASK_CONTROL_DISPATCH_SANDBOX_DRY_RUN_TASKBOOK.md);
- [Phase 18 Agent Task Control Dispatch Sandbox Dry-Run Closeout](PHASE_18_AGENT_TASK_CONTROL_DISPATCH_SANDBOX_DRY_RUN_CLOSEOUT.md);
- executable checks exposed through `npm run governance -- list`;
- the structured state-sync claim at `docs/current/state-sync-record.json`.


## Historical Audit Compatibility

Many deterministic audits bind source constraints to exact control-plane
phrases. The registry below preserves those lexical assertions so deletion of a
source boundary still fails CI. Phrases such as `current human authority`,
`active`, or `current operating entry point` are retained as audit markers
only; they are subordinate to the active authority model and capability table
above and do not reopen DGP, Provider, Desktop, recovery, or Agent OS roadmaps.
## Historical Authority Markers

| Surface | Role | Authority |
| --- | --- | --- |
| `docs/current/state-sync-record.json` | Machine-readable state-sync claim | Authority for state-sync audit. |
| `run-state-sync-audit.ts` + state-sync tests | State-sync boundary | Current human authority for a state consistency observation gate only; state-sync is not provider execute authorization, state-sync is not real Codex CLI authorization, state-sync is not workspace-write authorization, state-sync is not local command authorization, state-sync is not host executor authorization, state-sync is not sub-agent runtime authorization, state-sync is not tool runtime authorization, state-sync is not external-write authorization, state-sync is not evidence refresh authorization, state-sync is not push authorization, state-sync is not release authorization, state-sync git state is not execution authorization, clean worktree is not provider execution authorization, and state-sync policy v2 is not execution authorization. |
| `run-execution-boundary-current-surface-audit.ts` + current-surface tests | Execution authority lattice | Current code authority for `narrow_readonly_provider_dispatch_without_boundary_inheritance`; read-only provider dispatch does not inherit into host executor authorization, read-only provider dispatch does not inherit into sub-agent runtime authorization, read-only provider dispatch does not inherit into workspace-write authorization, and read-only provider dispatch does not inherit into release authorization. Codex CLI host does not authorize host executor or sub-agent runtime; sub-agent runtime does not invoke Codex CLI or provider execution; host executor does not execute provider or sub-agent runtime. |
| `docs/current/CURRENT_STATE.md` | Generated/operator display | Display only. |
| `.agent_board/*` | Handoff display | Display only. |
| This document | Current governance capability status | Current human authority. |
| `RELEASE_GATE_MATRIX.md` | PR/release gate policy | Current human authority. |
| `EVIDENCE_POLICY.md` | Evidence storage boundary | Current human authority. |
| `THREAT_MODEL.md` | Current risk and control map | Current human authority. |
| `CHANGE_CONTROL.md` | Required docs/tests for governance changes | Current human authority. |
| `WORKSPACE_WRITE_RELEASE_GATE.md` | Workspace-write promotion and block rules | Current human authority for workspace-write readiness. |
| `DOCS_AUTOMATION_SPEC.md` | Lightweight docs governance check contract | Current human authority for docs check scope. |
| `PHASE_6_CONTROLLED_EXECUTION_RUNTIME_HARDENING_BASELINE.md` | Runtime-hardening stage baseline | Current human authority for Phase 6 sequencing; not execution authorization. |
| `PHASE_6_CONTROLLED_EXECUTION_RUNTIME_HARDENING_CLOSEOUT.md` | Runtime-hardening stage closeout | Current human authority for Phase 6 completed capability status; not new execution authorization. |
| `PR_23B_CONTROLLED_READONLY_PROVIDER_EXECUTION_MINIMAL_SLICE.md` | Controlled read-only provider execution acceptance | Current human authority for the PR-23B minimal slice; not real Codex CLI authorization. |
| `PR_23C_EXECUTION_EVIDENCE_BINDING.md` | Controlled read-only execution evidence binding | Current human authority for PR-23C refs/hash evidence; not new execution authorization. |
| `PHASE_6_READONLY_PROVIDER_PERMIT_LIFECYCLE_HARDENING.md` | Read-only provider permit lifecycle hardening | Current human authority for expiration, nonce, replay, and store-failure acceptance coverage; not workspace-write authorization. |
| `PR_23D_WORKSPACE_WRITE_PERMIT_V2.md` | Workspace-write permit v2 schema and validators | Current human authority for permit v2 shape, validation, and consumption; not workspace-write execution authorization. |
| `PR_23E_WORKSPACE_WRITE_FAKE_CANARY_V2.md` | Workspace-write fake canary v2 | Current human authority for permit v2 fake-canary validation; not real workspace-write authorization. |
| `run-controlled-generic-workspace-write-acceptance.ts` + controlled generic workspace-write acceptance tests | Controlled generic workspace-write acceptance | Current acceptance evidence for the local workspace-write runner: it uses a temporary repository to preflight, execute, roll back, and replay-block explicit create/update/delete operations under permit v2, exact target allowlist, patch guard, and sanitized evidence. It does not call provider `execute`, does not spawn Codex CLI, does not authorize general / unbounded workspace-write, and does not perform external writes. |
| `run-controlled-generic-workspace-write-completion-audit.ts` + completion audit tests | Controlled generic workspace-write completion audit | Current read-only completion matrix for the controlled generic workspace-write path: it ties executor, provider runner, controlled dispatcher, host/desktop routing, Agent OS SDK/CLI/app-server prepare surfaces, public host facade structural dispatch types, committed acceptance evidence, and release gate posture together. It does not execute workspace-write, does not write evidence, and does not authorize general / unbounded workspace-write, provider `execute`, Codex CLI, or external writes. |
| `PR_22A_CONTROLLED_PROVIDER_EXECUTION_TASKBOOK.md` + controlled provider execution taskbook tests | Controlled provider execution taskbook boundary | Current human authority for the local-only minimal provider execution taskbook; taskbook is not provider execute authorization, taskbook is not real Codex CLI authorization, taskbook is not workspace-write authorization, taskbook is not local command authorization, taskbook is not protected remote authorization, taskbook is not host executor authorization, taskbook is not sub-agent runtime authorization, taskbook is not external-write authorization, taskbook is not release authorization, and taskbook is not secret change authorization. |
| `run-controlled-provider-execution-taskbook-review-audit.ts` + controlled provider execution taskbook review tests | Controlled provider execution taskbook review boundary | Current human authority for a git-state and artifact review gate only; review audit is not provider execute authorization, review audit is not real Codex CLI authorization, review audit is not workspace-write authorization, review audit is not local command authorization, review audit is not host executor authorization, review audit is not sub-agent runtime authorization, review audit is not external-write authorization, review audit is not release authorization, review audit git state is not execution authorization, and worktree clean is not provider execution authorization. |
| `CONTROLLED_PROVIDER_EXECUTION_DISPATCH_PREFLIGHT_MATRIX.md` + dispatch preflight tests | Controlled provider execution dispatch preflight boundary | Current human authority for `controlled_readonly_and_workspace_write_dispatch_preflight_matrix_only`; the matrix checks provider id, side effect class, sandbox, approval policy, permit, plan hash, registry, preflight artifact binding, operation manifest binding, real-execution guard, and governance stop conditions before runner handoff. It is not general provider execute authorization, is not general workspace-write authorization, and leaves final execution authority with the provider execution runner boundary. |
| `packages/governance-internal-controlled-provider-dispatcher/src/index.ts` + controlled provider dispatcher tests | Controlled provider execution dispatcher boundary | Current code authority for `controlled_readonly_and_workspace_write_pre_runner_dispatcher`; the dispatcher consumes read-only dispatch preflight artifacts, can prepare controlled workspace-write dispatch input by creating and storing the preflight artifact, and then consumes controlled workspace-write preflight artifacts, provider execution plan hashes, operation manifest hashes, provider registry selection, executor plans, permit validation, environment preflight artifact binding, and governance stop checks before calling the provider execution runner boundary. It does not call `provider.execute` directly for workspace-write, does not spawn Codex CLI, does not authorize general workspace-write, and leaves final execution authority with the provider execution runner boundary. |
| `run-readonly-productization-acceptance.ts` + read-only productization tests | Read-only productization boundary | Current human authority for a local read-only productization acceptance gate only; productization is not provider execute authorization, productization is not real Codex CLI authorization, productization is not workspace-write authorization, productization is not local command authorization, productization is not host executor authorization, productization is not sub-agent runtime authorization, productization is not tool runtime authorization, productization is not external-write authorization, productization is not evidence refresh authorization, productization is not release authorization, git state is not execution authorization, and worktree clean is not provider execution authorization. |
| `packages/governance-internal-strategy-router/src/index.ts` + strategy/provider-runner tests | Strategy router execution boundary | Current code authority for advisory strategy decisions and agent budgets only; `execute`, `verify`, and executor budget signals are routing advice, not runtime authorization. |
| `packages/execution-profiles/src/index.ts` + routing/desktop strategy tests | Execution profiles boundary | Current code authority for profile templates only; profile stages are not runtime steps, default roles are not sub-agent runtime authorization, defaultToolAccess is not tool runtime authorization, engineering_write is not workspace-write execution, protected_remote is not external-write authorization, allowParallel is not sub-agent runtime authorization, maxParallelAgents is not sub-agent spawn authorization, release-governance is not protected-remote authorization, and profile selection is not provider execution authorization. |
| `packages/policy-config/src/index.ts`, `routing-policy.yaml` + policy-config tests | Policy config boundary | Current code authority for policy schema and signal resolution only; hostRoutes are not host dispatch authorization, codex-cli host routes are not Codex CLI invocation, desktop host routes are not desktop runtime invocation, toolPolicies are not tool runtime authorization, protected_remote is not external-write authorization, approval rules are not approval grants, memory health block severity is not runtime block execution, memory guidance is not sub-agent runtime authorization, telemetry thresholds are not runtime authorization, and telemetry delivery windows are not host executor authorization. |
| `docs/governance/CAPABILITY_TAXONOMY_ESCALATION_POLICY.md` + capability taxonomy tests | Capability taxonomy boundary | Current code authority for capability classification and escalation policy only; bounded workspace-write canary is not workspace-write authorization, bounded workspace-write receipt is not execution authorization, scoped workspace-write class is not workspace-write execution, general workspace-write class is not execution authorization, general provider execution class is not provider execute authorization, external_write class is not external-write authorization, release_or_deploy class is not release authorization, secret_or_credential_change class is not secret access authorization, capability escalation policy is not runtime authorization, and canary evidence baseline is not execution authorization. |
| `run-capability-taxonomy-escalation-policy-audit.ts` + capability taxonomy escalation policy tests | Capability taxonomy escalation policy boundary | Current code authority for capability escalation policy only; escalation policy is not provider execute authorization, escalation policy is not Codex CLI authorization, escalation policy is not workspace-write authorization, escalation policy is not host executor authorization, escalation policy is not sub-agent runtime authorization, escalation policy is not tool runtime authorization, escalation policy is not external-write authorization, escalation policy is not release authorization, and escalation policy is not secret access authorization. |
| `packages/routing-engine/src/index.ts` + routing engine tests | Routing engine boundary | Current code authority for routing decision and provider grant only; routing decisions are not execution authorization, hostRoute is not host dispatch authorization, providerGrant is not provider execute authorization, codex-cli provider ids are not Codex CLI invocation, desktop provider ids are not desktop runtime invocation, sandboxMode is not workspace-write execution, toolAccess is not tool runtime authorization, approvalRequired is not approval grant, risk scores are not runtime authorization, and parallelism allowance is not sub-agent runtime authorization. |
| `packages/governance-internal-recovery-control/src/index.ts` + recovery-control and Phase 15-18 tests | Recovery control orchestration boundary | Current code authority for schemas, packets, reviews, and explicit injected witnesses only; schema statuses are not execution authorization, execution plans are not recovery execution authorization, execution gates are not runtime authorization, host executor reviews are not host dispatch authorization, dispatch authorization reviews are not adapter invocation authorization, task-control reviews are not sub-agent runtime authorization, sandbox witnesses are not production recovery execution, receipt statuses are not completion authorization, and recovery recommendations are not host executor authorization. |
| `packages/governance-internal-runtime-control/src/index.ts` + runtime-control tests | Runtime control boundary | Current code authority for runtime signal and escalation outcome only; runtime signals are not execution authorization, escalation outcomes are not provider execution authorization, upgrade_model is not model runtime invocation, open_circuit is not host dispatch authorization, failure counts are not recovery execution authorization, context pressure is not sub-agent runtime authorization, and high-risk signals are not Codex CLI authorization. |
| `packages/codex-cli-host/src/index.ts` + Codex CLI host tests | Codex CLI host boundary | Current code authority for the explicit Codex CLI host execution surface; read-only smoke stays `read-only` with approval policy `never`, workspace-write smoke requires explicit allowance and confirmation plus clean-worktree/rollback bindings, governance step-back blocks write sandbox before spawn, and this boundary audit does not itself spawn Codex CLI or write evidence. |
| `packages/public-api/src/*` + public API surface tests | Public API execution boundary | Current code authority for five named governance subpaths only: `protocol`, `policy`, `codex-adapter`, `evidence`, and manifest-only `provider`. There is no root, SDK, host, support, MCP/A2A, provider registry, generic provider execution, Codex CLI, sub-agent runtime, or host executor public export. Governed rollback remains receipt- and operator-permit-bound. |
| `packages/protocol-mcp/src/agent-os-local-runtime.ts` + Agent OS SDK/CLI/app-server wrappers and tests | Agent OS local runtime boundary | Current code authority for the local state and provider-plan runtime behind MCP, SDK, CLI, and app-server public surfaces; it may create local kernel records, approval permits, provider execution plans, may prepare controlled workspace-write dispatch input and sanitized preflight artifacts through explicit artifact-store / provider-registry / plan-store dependencies, and may delegate controlled workspace-write provider plans through an explicitly configured dispatcher with capability, approval, and local-mutation gates, but it does not authorize provider execute, Codex CLI, host executor, sub-agent runtime, general workspace-write, workspace-write through `provider.execute`, live MCP server connection, live HTTP server startup, or network access by itself. |
| `packages/governance-internal-approval-permit/src/index.ts` + approval permit tests | Approval permit boundary | Current code authority for permit creation, validation, revocation, and store only; valid permits are not provider execution authorization, valid permits are not Codex CLI authorization, valid permits are not sub-agent runtime authorization, valid permits are not host executor authorization, shell capability scopes are not shell execution, external capability scopes are not external-write execution, and approval permit store persistence is not workspace-write execution. |
| `packages/governance-internal-approval-gate/src/index.ts` + approval gate tests | Approval gate boundary | Current code authority for approval requirement evaluation only; approval not_required status is not execution authorization, approval resolution is not provider execution authorization, approval resolution is not Codex CLI authorization, approval resolution is not sub-agent runtime authorization, approval resolution is not host executor authorization, and protected branch and dirty workspace signals are not workspace-write execution. |
| `APPROVAL_CONSUMPTION_DISPATCH_AUDIT_MATRIX.md` + approval consumption dispatch tests | Approval consumption dispatch boundary | Current human authority for approval consumption dispatch matrix only; matrix is not provider execute authorization, matrix is not real Codex CLI authorization, matrix is not workspace-write authorization, matrix is not local command authorization, matrix is not host executor authorization, matrix is not sub-agent runtime authorization, matrix is not external-write authorization, matrix is not release authorization, approval permit consumption is not provider execution authorization, host dispatcher precondition is not provider execute authorization, and redaction coverage is not runtime authorization. |
| `run-approval-consumption-dispatch-matrix-audit.ts` + approval consumption dispatch matrix tests | Approval consumption dispatch matrix boundary | Current human authority for a git-state and artifact matrix gate only; matrix audit is not provider execute authorization, matrix audit is not real Codex CLI authorization, matrix audit is not workspace-write authorization, matrix audit is not local command authorization, matrix audit is not host executor authorization, matrix audit is not sub-agent runtime authorization, matrix audit is not tool runtime authorization, matrix audit is not external-write authorization, matrix audit is not release authorization, matrix audit git state is not execution authorization, and worktree clean is not provider execution authorization. |
| `packages/admission-control/src/index.ts` + admission-control tests | Admission control boundary | Current code authority for admission status and requirement derivation only; accepted status is not execution authorization, needs_approval status is not approval grant, required approvals are not provider execution authorization, required approvals are not Codex CLI authorization, required approvals are not sub-agent runtime authorization, required approvals are not host executor authorization, external capabilities are not external-write execution, and file write capabilities are not workspace-write execution. |
| `packages/delegation-policy/src/index.ts` + delegation-policy tests | Delegation policy boundary | Current code authority for delegation level, approval requirement, and recovery action filtering only; full_delegation is not execution authorization, requiresApproval false is not execution authorization, approved proposals are not runtime authorization, applied proposals are not provider execution authorization, filtered recovery actions are not host executor authorization, recovery action lists are not recovery execution, historical trust is not runtime authorization, recorded resumes are not runtime invocation, and delegation file-store persistence is not workspace-write execution. |
| `packages/governance-internal-provider-execution-runner/src/index.ts` + provider execution runner tests | Provider execution runner boundary | Current code authority for the controlled read-only provider execute gate and controlled workspace-write local runner; `provider.execute` can be invoked only for explicit `codex-cli` + `read_only` + `read-only` controlled mode with permit, executor plan, preflight artifact binding, real execution guard, and governance state checks, while controlled workspace-write must use `runWorkspaceWriteExecution` and must not call `provider.execute` or real Codex CLI. |
| `packages/execution-eligibility/src/index.ts` + execution eligibility tests | Execution eligibility boundary | Current code authority for admission/capability/permit decision only; eligible status is not execution authorization, valid approval permits are not provider execution authorization, capability grants are not runtime invocation, permit-store reads are scoped to task/run/principal/plan/policy, and the boundary does not create provider plans or invoke provider execute, Codex CLI, sub-agent runtime, host executor, host dispatch, shell/process, workspace-write, or external write. |
| `packages/governance-internal-execution-observation/src/index.ts` + execution observation tests | Execution observation boundary | Current code authority for sanitized task-scoped observation records only; observation status is not execution authorization, succeeded observations are not completion authorization, failed observations are not recovery authorization, evidence refs are not runtime invocation, observation refs are not replay authorization, and file-store persistence is audit-record storage rather than workspace-write execution. |
| `packages/governance-failure-reducer/src/index.ts` + governance failure reducer tests | Governance failure reducer boundary | Current code authority for a pure failure-to-governance-state reducer only; execution failures are not recovery authorization, strategy decisions are not runtime authorization, arbitration packets are not recovery execution, recovery recommendations are not host executor authorization, anomaly records are not runtime invocation, evidence refs are not replay authorization, risk scores are not provider execution authorization, and reducer state updates are not workspace-write execution. |
| `packages/task-graph/src/index.ts` + task graph tests | Task graph boundary | Current code authority for structural task graph state only; node statuses are not execution authorization, graph completion is not runtime completion, dependency edges are not scheduler dispatch, conflict edges are not runtime block execution, checkpoint nodes are not rollback execution, graph deltas are not workspace rollback authorization, rollbackToCheckpoint is not host executor authorization, branch merges are not git merge or workspace-write, and task graph file-store persistence is not workspace-write execution. |
| `packages/scheduler/src/index.ts` + scheduler tests | Scheduler boundary | Current code authority for a queue and execution lease state machine only; queued status is not dispatch authorization, leased status is not execution authorization, active leases are not provider execute authorization, worker ids are not host executor or sub-agent identity authorization, releaseLease is not runtime completion proof, failLease is not recovery execution, expired leases are not retry execution, exhausted status is not runtime block execution, scheduler file-state persistence is not workspace-write execution, and scheduler file locks are not shell/process execution. |
| `packages/execution-planner/src/index.ts` + execution planner tests | Execution planner boundary | Current code authority for provider execution plan only; planned status is not provider execution authorization, codex-cli provider selection is not Codex CLI invocation, remote-agent provider selection is not sub-agent runtime invocation, workspace-write side-effect classification is not workspace-write execution, and local plan store writes are limited to provider execution plan state. |
| `packages/provider-registry/src/index.ts` + provider registry tests/acceptance | Provider registry boundary | Current code authority for catalog selection, attestation, and manifest store only; selected providers are not execution authorization, provider grant selection is not provider execute authorization, routing decision selection is not Codex CLI authorization, registered remote-agent providers are not sub-agent runtime authorization, and manifest-store persistence is not workspace-write execution. |
| `packages/protocol-mcp/src/agent-os-server-manifest.ts` + Agent OS MCP server manifest tests | Agent OS MCP server manifest boundary | Current code authority for manifest only; no runtime. runtimeImplemented false is not a live MCP server, tool manifests are not tool runtime authorization, requiredCapabilities are not capability grants, approvalRequired is not approval grant, local_write side effects are not workspace-write execution, and provider planning and approval permit output schemas are not provider execution authorization. |
| `packages/protocol-mcp/src/index.ts` + protocol MCP provider skeleton tests | Protocol MCP provider skeleton boundary | Current code authority for protocol mapping and disabled provider skeleton only; server refs are not live MCP server connections, commandRef is not a shell command, endpointRef is not a network call, tool manifests are not tool runtime authorization, invocation plans are not tool execution authorization, fake providers are not live MCP servers, invoke remains disabled, unknown side effects are not auto-approved, and allowed tools are not MCP invocation authorization. |
| `packages/protocol-a2a/src/index.ts` + protocol A2A tests | Protocol A2A remote provider skeleton boundary | Current code authority for agent card, task, artifact mapping and disabled remote provider skeleton only; endpoint refs are not network calls, agent cards are not remote runtime authorization, task skeletons are not remote execution authorization, artifact URIs are not fetched by the skeleton, remote providers remain disabled, fake transports are not live network services, anonymous remote invocation remains rejected, auth schemes are not capability grants, and remote-agent provider manifests are not sub-agent runtime authorization. |
| `packages/agent-os-sdk/src/index.ts` + Agent OS SDK tests | Agent OS SDK boundary | Current code authority for SDK method calls to local MCP runtime only; SDK typed inputs may carry controlled workspace-write prepare or dispatch payloads to the local runtime when explicit runtime dependencies are configured. SDK call options are not capability grants, approved mutating tools are not tool runtime authorization, allow-local-mutation is not workspace-write execution, preferred provider is not Codex CLI invocation, local runtime calls are not provider execution authorization, approval permit issue and consumption are not provider execution, and real provider execution remains uninvoked. |
| `packages/agent-os-cli/src/index.ts` + Agent OS CLI tests | Agent OS CLI boundary | Current code authority for argv parsing to local MCP runtime only; CLI `--prepare-json` and `--dispatch-input-json` may carry controlled workspace-write payloads to the local runtime when explicit runtime dependencies are configured. CLI grant flags are not capability grants, approve-tool flags are not tool runtime authorization, allow-local-mutation is not workspace-write execution, preferred provider is not Codex CLI invocation, parsed commands are not provider execution authorization, approval permit issue and consumption are not provider execution, sanitized argv must not expose raw secrets, and the CLI wrapper does not spawn Codex CLI. |
| `packages/agent-os-app-server/src/index.ts` + Agent OS app-server tests | Agent OS app-server wrapper boundary | Current code authority for HTTP-like request routing to local MCP runtime only; `/agent-os/workspace-write/dispatch` may carry controlled workspace-write prepare or dispatch payloads to the local runtime when explicit server-side dependencies are configured. HTTP-like routes are not live network servers, request envelopes are not capability grants, client-supplied gate fields remain ignored, status codes are not host executor receipts, approval permit issue and consumption are not provider execution, live HTTP servers remain unimplemented, network access remains absent, and real provider execution remains uninvoked. |
| `packages/agent-os-sdk/src/index.ts`, `packages/agent-os-cli/src/index.ts`, `packages/agent-os-app-server/src/index.ts` + Agent OS public surface tests | Agent OS public surfaces boundary | Current code authority for public surface to local MCP runtime only; SDK calls, CLI argv, and app-server request bodies may carry controlled workspace-write prepare or dispatch payloads to the local runtime when explicit dependencies are configured. SDK calls are not provider execution authorization, CLI grant and approve-tool flags are not provider, tool runtime, Codex CLI, or sub-agent runtime authorization, allow-local-mutation is not workspace-write execution, preferred provider is not Codex CLI invocation, app-server request envelopes are not capability grants, HTTP-like routes are not live network servers, and status codes are not host executor receipts. |
| `packages/provider-core/src/index.ts` + provider-core registry/planner tests | Provider-core execution primitives | Current code authority for provider manifest, permit, registry, and planning primitives only; remote-agent, tool, and workspace-write primitives are not runtime authorization. |
| `packages/preflight/src/index.ts` + preflight tests | Preflight boundary | Current code authority for pre-execution signal evaluation only; preflight ok is not execution authorization, missing tool checks are not tool runtime authorization, auth availability is not provider execution authorization, workspace clean is not workspace-write authorization, protected branch checks are not workspace-write execution, memory overview is not runtime authorization, memory health status is not sub-agent runtime authorization, memory warnings are not host executor authorization, and memory blocking issues are not provider execution authorization. |
| `packages/tool-registry/src/index.ts`, `packages/tool-invocation-planner/src/index.ts` + tool registry/planner tests | Tool invocation planner boundary | Current code authority for tool manifest and invocation plan only; planned tool invocation status is not runtime invocation, remote.agent.invoke is not sub-agent runtime authorization, external-write tool manifests are not external-write authorization, and approval permits are not tool runtime authorization. |
| `packages/desktop-agent-strategy/src/index.ts` + desktop agent strategy tests | Desktop agent strategy boundary | Current code authority for agent assignment and ownership plan only; parallel plans are not sub-agent runtime authorization, worker assignments are not runtime invocation, write mode is not workspace-write execution, ownership targets are not workspace-write authorization, maxAgents is not sub-agent spawn authorization, read-only analyst assignments are not provider execution authorization, and strategy reasons are not execution gates. |
| `packages/desktop-decision-runner/src/index.ts` + routing/agent-strategy tests | Desktop decision runner boundary | Current code authority for a decision package only; ready status, provider selection, provider grants, agent strategy, governance strategy `execute` action, and desktop execution-plan `authorized` markers are not runtime authorization and do not invoke desktop primitives, host dispatch, provider execute, Codex CLI, sub-agent runtime, shell/process, workspace-write, or external write by themselves. |
| `packages/final-host-locator/src/index.ts` + final host locator tests | Final host locator boundary | Current code authority for source candidate and pre-mapping only; ready_for_mapping is not host execution authorization, packaged runtimes and reference hosts cannot become the final host source by themselves, path probes are read-only and bounded, and the locator does not invoke host executor, desktop host client, host dispatch, provider execute, Codex CLI, sub-agent runtime, shell/process, workspace-write, or external write. |
| `packages/host-dispatcher/src/index.ts` + host-dispatcher tests | Host dispatcher provider boundary | Current code authority for controlled read-only and controlled workspace-write provider dispatch. Direct host-side provider execute remains `codex-cli + read_only + read-only` only, formal read-only dispatch requires registry and metadata, controlled workspace-write dispatch input preparation delegates to the controlled dispatcher preflight artifact path, and controlled workspace-write execution delegates to the controlled dispatcher/local runner without provider execute. |
| `packages/codex-desktop-bindings/src/index.ts` + desktop bridge tests | Codex desktop bridge boundary | Current code authority for explicit injected desktop host bridge wiring; runtime tool invocation is not default-authorized and shell governance and patch hashing remain required. |
| `packages/codex-desktop-live-host/src/index.ts` + live-host tests | Codex desktop live host boundary | Current code authority for explicit current host runtime and memory bundle composition; bundle creation requires a current host object with all runtime and required memory methods before bundle creation. |
| `packages/codex-memory-mcp-client/src/index.ts`, `packages/codex-memory-adapter/src/index.ts` + memory MCP client tests | Codex memory MCP client boundary | Current code authority for explicit MCP HTTP memory transport only; MCP HTTP calls are not provider execution, MCP HTTP calls are not host executor authorization, recordMemory is not workspace-write execution, searchMemory is not sub-agent runtime invocation, memoryOverview is not runtime authorization, and bearer tokens are not execution authorization. |
| `packages/codex-memory-host-client/src/index.ts`, `packages/codex-memory-adapter/src/index.ts` + memory host-client tests | Codex memory host client boundary | Current code authority for explicit injected memory operations only; memory operation calls are not host executor authorization, recordMemory is not workspace-write execution, searchMemory is not sub-agent runtime invocation, memoryOverview is not runtime authorization, and MCP tool-style adapter is not default host lookup. |
| `packages/desktop-host-client/src/index.ts` + public host API/client tests | Desktop host client boundary | Current code authority for the desktop host client facade; it delegates `run` and `resume` to the desktop live adapter, delegates operator action executor authorization/dispatch to recovery-control review or explicit injected dispatch, may delegate controlled workspace-write provider plans to the host dispatcher, and does not authorize Codex CLI, general provider execution, workspace-write through `provider.execute`, general workspace-write, sub-agent runtime, shell/process, or external write by itself. |
| `packages/desktop-live-adapter/src/index.ts` + live adapter tests | Desktop live adapter dispatch boundary | Current code authority for route-separated host dispatch or desktop bridge execution; codex-cli routes do not invoke desktop bridge handlers and blocked decisions do not execute. |
| `packages/host-client-example/src/index.ts` + example host client tests | Host client example boundary | Current code authority for the example-only host facade; it delegates `run` and `resume` to the desktop live adapter, uses simulated desktop primitive envelopes for the default example bridge, and does not authorize Codex CLI, provider, sub-agent runtime, host executor, real shell/process, workspace-write, or external write. |
| `packages/host-client-example/src/target-host-*.ts` + target host tests | Target host embedding boundary | Current code authority for explicit target-host contract and starter only; placeholder host methods are not real execution, scaffold ready status is not execution authorization, createBundle requires a fully wired explicit host, createBundle is not host executor authorization, and directive builders are not shell authorization. |
| `PHASE_7_RUNTIME_OPERATOR_ACTIONABILITY_CLOSEOUT.md` | Runtime operator actionability closeout | Current human authority for preflight governance, operator action envelopes, summaries, host-client surfaces, and evidence resolution; not execution authorization. |
| `PHASE_8_OPERATOR_ACTION_LIFECYCLE_CLOSEOUT.md` | Operator action lifecycle closeout | Current human authority for operator action receipt shape, validation, expiry, replay blocking, lifecycle stores, and lockdown receipt policy; not execution authorization. |
| `PHASE_9_OPERATOR_ACTION_HOST_LIFECYCLE_CLOSEOUT.md` | Operator action host lifecycle closeout | Current human authority for host/client receipt consumption, receipt authoring, and lifecycle state surfaces; not execution authorization. |
| `PHASE_10_OPERATOR_ACTION_EXECUTOR_GATE_CLOSEOUT.md` | Operator action executor gate closeout | Current human authority for the plan-only operator action executor gate, durable receipt proof binding, lifecycle binding, action allowlists, checkpoint propagation, and sanitized evidence summaries; not execution authorization. |
| `PHASE_11_OPERATOR_ACTION_HOST_EXECUTOR_BOUNDARY_TASKBOOK.md` | Operator action host executor boundary taskbook | Current human authority for the next non-executing authorization packet and injected host executor descriptor boundary; not execution authorization. |
| `PHASE_11_OPERATOR_ACTION_HOST_EXECUTOR_BOUNDARY_CLOSEOUT.md` | Operator action host executor boundary closeout | Current human authority for the implemented non-executing host executor descriptor, authorization packet, lifecycle binding, and review result boundary; not execution authorization. |
| `PHASE_12_OPERATOR_ACTION_HOST_CLIENT_REVIEW_SURFACE_CLOSEOUT.md` | Operator action host client review surface closeout | Current human authority for exposing the Phase 11 non-executing review result through `DesktopHostClient` current lifecycle state; not execution authorization. |
| `PHASE_13_OPERATOR_ACTION_HOST_EXECUTOR_DISPATCH_TASKBOOK.md` | Operator action host executor dispatch taskbook | Current authorization stop for any future side-effecting recovery action dispatch implementation; not execution authorization. |
| `PHASE_13_OPERATOR_ACTION_HOST_EXECUTOR_DISPATCH_CLOSEOUT.md` | Operator action host executor dispatch closeout | Current human authority for the implemented dry-run and explicit injected-executor dispatch boundary; real recovery dispatch remains blocked by default. |
| `PHASE_13_AGENT_BACKED_RECOVERY_EXECUTOR_BOUNDARY.md` | Agent-backed recovery executor boundary | Current human authority for host-provided / agent-backed recovery executor semantics and sandbox-only contract proof; not production recovery execution authorization. |
| `PHASE_14_AGENT_EXECUTOR_RECEIPT_CONTRACT.md` | Agent executor receipt contract | Current human authority for non-executing executor receipt status normalization; not real recovery execution authorization. |
| `PHASE_15_AGENT_EXECUTOR_ADAPTER_AUTHORIZATION_TASKBOOK.md` | Agent executor adapter authorization taskbook | Current human authority for future adapter pre-execution review requirements; not Codex CLI, provider, sub-agent runtime, shell, workspace-write, or production recovery execution authorization. |
| `PHASE_15_AGENT_EXECUTOR_ADAPTER_REVIEW_ONLY_CLOSEOUT.md` | Agent executor adapter review-only closeout | Current human authority for implemented review-only adapter descriptor and packet readiness; not Codex CLI, provider, sub-agent runtime, shell, workspace-write, or production recovery execution authorization. |
| `PHASE_15_AGENT_EXECUTOR_ADAPTER_SANDBOX_CONTRACT_CLOSEOUT.md` | Agent executor adapter sandbox contract closeout | Current human authority for implemented sandbox-only adapter contract witness; not Codex CLI, provider, sub-agent runtime, shell, workspace-write, or production recovery execution authorization. |
| `PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_AUTHORIZATION_TASKBOOK.md` | Agent executor adapter dispatch authorization taskbook | Current human authority for future adapter dispatch authorization requirements; design-only and not Codex CLI, provider, sub-agent runtime, shell/process, workspace-write, external-write, or production recovery authorization. |
| `PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_AUTHORIZATION_REVIEW_ONLY_CLOSEOUT.md` | Agent executor adapter dispatch authorization review-only closeout | Current human authority for implemented review-only dispatch authorization packet binding; not adapter invocation, Codex CLI, provider, sub-agent runtime, shell/process, workspace-write, external-write, or production recovery authorization. |
| `PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_SANDBOX_DRY_RUN_TASKBOOK.md` | Agent executor adapter dispatch sandbox dry-run taskbook | Implemented planning authority for the sandbox-only dispatch dry-run stop; not Codex CLI, provider, sub-agent runtime, shell/process, workspace-write, external-write, or production recovery authorization. |
| `PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_SANDBOX_DRY_RUN_CLOSEOUT.md` | Agent executor adapter dispatch sandbox dry-run closeout | Current human authority for the implemented sandbox-only dispatch dry-run boundary; not real recovery execution authorization. |
| `PHASE_17_AGENT_TASK_CONTROL_DISPATCH_BOUNDARY_TASKBOOK.md` | Agent task control dispatch boundary taskbook | Current human authority for future `agent_task_control` / `agent_context_only` dispatch authorization requirements; taskbook boundary and not Codex CLI, provider, sub-agent runtime, shell/process, workspace-write, external-write, or production recovery authorization. |
| `PHASE_17_AGENT_TASK_CONTROL_DISPATCH_AUTHORIZATION_REVIEW_ONLY_CLOSEOUT.md` | Agent task control dispatch authorization review-only closeout | Current human authority for implemented review-only `agent_task_control` + `agent_context_only` packet binding; not adapter invocation, Codex CLI, provider, sub-agent runtime, shell/process, workspace-write, external-write, or production recovery authorization. |
| `PHASE_18_AGENT_TASK_CONTROL_DISPATCH_SANDBOX_DRY_RUN_TASKBOOK.md` | Agent task control dispatch sandbox dry-run taskbook | Current taskbook authority for a future sandbox-only task-control contract witness; planning-only and not adapter invocation, Codex CLI, provider, sub-agent runtime, shell/process, workspace-write, external-write, or production recovery authorization. |
| `PHASE_18_AGENT_TASK_CONTROL_DISPATCH_SANDBOX_DRY_RUN_CLOSEOUT.md` | Agent task control dispatch sandbox dry-run closeout | Current human authority for the implemented sandbox-only task-control contract witness; not Codex CLI, provider, sub-agent runtime, shell/process, workspace-write, external-write, or production recovery authorization. |
| `PR_*`, `FUTURE_*`, closeouts, packets | Historical evidence | Evidence only unless linked by a current authority document. |

## Historical Capability Markers

| Capability | Status | Real execution allowed | Current rule |
| --- | --- | ---: | --- |
| Protocol V1 contracts | active | N/A | Stable package surface; keep changes tested and reviewable. |
| Read-only dry run | active | No | Safe default for local inspection, demos, and deterministic tests. |
| Runtime governance observation | active | No by itself | May record sanitized observations, refs, anomalies, and operator actions. |
| Runtime operator actionability | active | No by itself | Preflight blocks, runtime failures, desktop live results, and host clients expose stable operator action envelopes/summaries and sanitized evidence-resolution summaries. |
| Runtime operator action lifecycle receipts | active | No by itself | Operator decisions can be validated and durably consumed against task-scoped action refs, optional envelope hashes, timestamps, expiry, replay state, store replay barriers, and lockdown receipt policy. |
| Runtime operator action host lifecycle | active | No by itself | Host clients can author, consume, and query current operator action lifecycle state without executing recovery actions. |
| Runtime operator action executor gate | active / plan-only | No | Phase 10 can produce a checkpoint-preserving plan only after durable receipt proof, lifecycle binding, and action allowlist checks pass. |
| Runtime operator action host executor boundary | active / non-executing | No | Phase 11 binds planned gates, lifecycle state, authorization packets, and injected host executor descriptors for review readiness only; recovery action dispatch remains closed. |
| Runtime operator action host-client executor review surface | active / non-executing | No | Phase 12 exposes Phase 11 review through `DesktopHostClient.reviewCurrentOperatorActionHostExecutorAuthorization()` using current lifecycle state only; bridge, dispatcher, provider, Codex CLI, and workspace-write paths remain untouched. |
| Runtime operator action host executor dispatch | active / guarded | No by default | Phase 13 adds dry-run and explicit injected-executor dispatch control. Repository validation uses fake injected executors only; real `resume`, `rollback`, `abort`, or `fork` dispatch remains a separate authorization stop. |
| Agent-backed recovery executor boundary | active / sandbox proof only | No production execution | Phase 13 follow-up defines host-provided / agent-backed executor semantics and a sandbox-only reference executor contract proof. It does not add business recovery logic, Codex CLI, provider, shell, external write, or arbitrary workspace-write execution. |
| Agent executor receipt contract | active / non-executing | No | Phase 14 normalizes injected executor receipt statuses as `accepted`, `running`, `completed`, `failed`, `refused`, or `aborted` with sanitized reason codes and refs. It does not add an executor adapter, Codex CLI invocation, provider call, shell/process execution, external write, or workspace-write execution. |
| Agent executor adapter authorization | active / sandbox contract witness | No | Phase 15 records future adapter packet requirements and implements review-only readiness plus a sandbox-only reference adapter contract run. The contract run is limited to `sandbox_reference_adapter`, explicit injection, sanitized audit, and sandbox-contained test artifacts; Codex CLI, provider, sub-agent runtime, shell/process execution, external write, workspace-write, and production recovery execution remain blocked. |
| Agent executor adapter dispatch authorization | active / review-only + sandbox dry-run | No real execution | Phase 16 defines dispatch authorization packets, implements review-only packet/readiness binding for `review_only` + `none`, and implements a sandbox-only dry-run for `sandbox_contract` + `sandbox_only`. It does not authorize Codex CLI, provider, sub-agent runtime, shell/process execution, workspace-write, external write, or production recovery execution. |
| Agent executor adapter dispatch sandbox dry-run | active / sandbox contract witness | No real execution | Phase 16 can call only an explicitly injected Phase 15 `sandbox_reference_adapter` contract witness after the sandbox dry-run packet, readiness hash, audit sink, evidence sink, and sandbox contract packet all bind. It records sanitized audit/evidence and remains non-production proof only. |
| Sub-agent runtime execution boundary | blocked / review identities only | No | `sub_agent_adapter` may appear only as a review identity with `review_only`, `invocationSupported: false`, and `none` side effects. Executable proofs remain limited to injected `sandbox_reference_adapter` and `sandbox_task_control_adapter` witnesses; no sub-agent process or runtime invocation is authorized. |
| Agent task control dispatch authorization | active / review-only | No | Phase 17 defines and implements non-executing `agent_task_control` + `agent_context_only` authorization review. It binds Phase 10/11/15/16 identities, host-agent refs, context refs, idempotency, timeout, and sink identities without invoking an adapter, Codex CLI, provider, sub-agent runtime, shell/process execution, workspace-write, external write, or production recovery execution. |
| Agent task control dispatch sandbox dry-run | active / sandbox contract witness | No real execution | Phase 18 binds a ready Phase 17 `agent_task_control` + `agent_context_only` authorization review to an explicitly injected `sandbox_task_control_adapter`. It records sanitized audit/evidence and remains non-production proof only; Codex CLI, provider, sub-agent runtime, shell/process execution, workspace-write, external write, and production recovery execution remain blocked. |
| Controlled read-only real execution | guarded / productized | Yes, narrow | Requires [read-only controlled execution runbook](runbooks/READONLY_CONTROLLED_EXECUTION_RUNBOOK.md), [PR-23B minimal slice](PR_23B_CONTROLLED_READONLY_PROVIDER_EXECUTION_MINIMAL_SLICE.md), [PR-23C evidence binding](PR_23C_EXECUTION_EVIDENCE_BINDING.md), explicit controlled mode, injected execution dependency, permit/preflight metadata, stable evidence refs/hashes, and no hidden provider path. |
| Execution authority lattice | active / current-surface invariant | No broader inheritance | The current surface audit records `narrow_readonly_provider_dispatch_without_boundary_inheritance`: the narrow read-only provider runner and host dispatcher allowance does not promote host executor authorization, sub-agent runtime authorization, workspace-write authorization, real Codex CLI authorization, external-write authorization, or release authorization. Codex CLI host does not authorize host executor or sub-agent runtime; sub-agent runtime does not invoke Codex CLI or provider execution; host executor does not execute provider or sub-agent runtime. |
| Read-only productization boundary | active / local read-only productization acceptance gate only | No | The read-only productization acceptance chain records a clean local review of package scripts, evidence files, readiness matrix closure, productization docs, and roadmap markers. Productization is not provider execute authorization, real Codex CLI authorization, workspace-write authorization, local command authorization, host executor authorization, sub-agent runtime authorization, tool runtime authorization, external-write authorization, evidence refresh authorization, release authorization, or execution authorization by itself; git state is not execution authorization and worktree clean is not provider execution authorization. |
| Strategy router execution boundary | active / advisory budget signal only | No | `governance-internal-strategy-router` emits strategy decisions, action families, and agent budgets only. `execute`, `verify`, and executor budget signals are routing advice, not runtime authorization; the router does not invoke Codex CLI, provider execution, sub-agent runtime, host executor, shell/process, workspace-write, or external write. |
| Execution profiles boundary | active / profile templates only | No | `execution-profiles` defines stage, role, toolAccess, parallelism, and max-agent templates for routing and desktop strategy. Profile stages, default roles, defaultToolAccess, engineering_write, protected_remote, allowParallel, maxParallelAgents, release-governance, and profile selection are not runtime steps, sub-agent runtime authorization, tool runtime authorization, workspace-write execution, external-write authorization, sub-agent spawn authorization, protected-remote authorization, provider execution authorization, Codex CLI invocation, host executor dispatch, shell/process execution, or approval grants by themselves. |
| Policy config boundary | active / policy schema and signal resolution only | No | `policy-config` loads and validates `routing-policy.yaml`, resolves memory health packs, and resolves telemetry presets. hostRoutes, codex-cli host routes, desktop host routes, toolPolicies, protected_remote, approval rules, memory health block severity, memory guidance, telemetry thresholds, and telemetry delivery windows are not host dispatch authorization, Codex CLI invocation, desktop runtime invocation, tool runtime authorization, external-write authorization, approval grants, runtime block execution, sub-agent runtime authorization, runtime authorization, host executor authorization, provider execute, shell/process execution, workspace-write execution, or external write by themselves. |
| Capability taxonomy boundary | active / capability classification and escalation policy only | No | The capability taxonomy records read_only, bounded workspace-write canary/receipt, scoped workspace-write, general workspace-write, general provider execution, external_write, release_or_deploy, and secret_or_credential_change classes. Bounded workspace-write canary, bounded workspace-write receipt, scoped workspace-write class, general workspace-write class, general provider execution class, external_write class, release_or_deploy class, secret_or_credential_change class, capability escalation policy, and canary evidence baseline are not workspace-write authorization, execution authorization, workspace-write execution, provider execute authorization, external-write authorization, release authorization, secret access authorization, runtime authorization, Codex CLI invocation, host executor dispatch, shell/process execution, provider execute, or external write by themselves. |
| Capability taxonomy escalation policy boundary | active / capability escalation policy only | No | The escalation policy is not provider execute authorization, Codex CLI authorization, workspace-write authorization, host executor authorization, sub-agent runtime authorization, tool runtime authorization, external-write authorization, release authorization, secret access authorization, runtime block execution, runtime authorization, or execution authorization by itself. |
| Routing engine boundary | active / routing decision and provider grant only | No | `routing-engine` classifies task routing into selected model, toolAccess, executionProfile, approvalRequired signals, hostRoute, parallelism, and providerGrant metadata. Routing decisions, hostRoute, providerGrant, codex-cli provider ids, desktop provider ids, sandboxMode, toolAccess, approvalRequired, risk scores, and parallelism allowance are not provider execute, Codex CLI invocation, desktop runtime invocation, sub-agent runtime authorization, host executor dispatch, tool runtime invocation, shell/process execution, workspace-write execution, external write, execution authorization, host dispatch authorization, approval grant, or provider execute authorization by themselves. |
| Recovery control orchestration boundary | active / schemas, packets, reviews, and explicit injected witnesses only | No by itself | `governance-internal-recovery-control` defines recovery recommendations, operator action envelopes, receipts, plan-only execution gates, host executor reviews, adapter readiness, dispatch authorization reviews, task-control reviews, and explicit injected sandbox witnesses. Schema statuses, execution plans, execution gates, host executor reviews, dispatch authorization reviews, task-control reviews, sandbox witnesses, receipt statuses, and recovery recommendations are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, adapter invocation authorization, shell/process execution, workspace-write execution, external write, completion authorization, production recovery execution, or recovery execution authorization by themselves. |
| Runtime control boundary | active / runtime signal and escalation outcome only | No | `governance-internal-runtime-control` derives runtime signals from governance state and evaluates escalation outcomes such as `none`, `upgrade_model`, and `open_circuit`. Runtime signals, escalation outcomes, `upgrade_model`, `open_circuit`, failure counts, context pressure, and high-risk signals are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, model runtime invocation, shell/process execution, workspace-write execution, external write, execution authorization, recovery execution authorization, or provider execution authorization by themselves. |
| Execution eligibility boundary | active / admission/capability/permit decision only | No | `execution-eligibility` evaluates admission status, capability grants, and approval permits only. `eligible` status, valid permits, capability grants, and scoped permit-store reads are not provider execute, Codex CLI, sub-agent runtime, host executor, host dispatch, shell/process, workspace-write execution, external write, or provider plan creation authorization by themselves. |
| Execution observation boundary | active / sanitized task-scoped observation record only | No | `governance-internal-execution-observation` records and resolves observation refs for evidence. `started`, `succeeded`, `failed`, `degraded`, and `blocked` statuses, evidence refs, and file-store persistence are not execution authorization, completion authorization, recovery authorization, replay authorization, provider execute, Codex CLI, sub-agent runtime, host executor, host dispatch, shell/process, workspace-write execution, or external write by themselves. |
| Governance failure reducer boundary | active / pure failure-to-governance-state reducer only | No | `governance-failure-reducer` transforms an execution failure into a copied governance state, anomaly, risk score, strategy decision, and arbitration packet. Execution failures, strategy decisions, arbitration packets, recovery recommendations, anomaly records, evidence refs, risk scores, and reducer state updates are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, tool runtime invocation, shell/process execution, workspace-write execution, external write, or recovery execution by themselves. |
| Task graph boundary | active / structural task graph state only | No | `task-graph` records graph nodes, edges, branch ownership, checkpoint deltas, rollback snapshots, branch merges, and optional graph file-store state. Node statuses, graph completion, dependency edges, conflict edges, checkpoint nodes, graph deltas, rollbackToCheckpoint, branch merges, and task graph file-store persistence are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, tool runtime invocation, shell/process execution, workspace-write execution, external write, scheduler dispatch, runtime completion, git merge, or rollback execution by themselves. |
| Scheduler boundary | active / queue and execution lease state machine only | No | `scheduler` records queued runs, active/released/expired/failed leases, retry attempts, max-attempt exhaustion, optional scheduler file-state, and file locks. Queued status, leased status, active leases, worker ids, releaseLease, failLease, expired leases, exhausted status, scheduler file-state persistence, and scheduler file locks are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, tool runtime invocation, shell/process execution, workspace-write execution, external write, dispatch authorization, execution authorization, runtime completion proof, retry execution, runtime block execution, or recovery execution by themselves. |
| Execution planner boundary | active / provider execution plan only | No by itself | `execution-planner` creates hashed provider execution plans and may write only local provider execution plan store state. `planned` status, codex-cli provider selection, remote-agent provider selection, workspace-write side-effect classification, and local plan persistence are not provider execute, Codex CLI, sub-agent runtime, host executor, host dispatch, shell/process, workspace-write execution, or external write authorization. |
| Approval permit boundary | active / permit creation, validation, revocation, and store only | No | `governance-internal-approval-permit` creates, validates, revokes, hashes, clones, and stores scoped approval permits. Valid permits, matched capability scopes, shell capability scopes, external capability scopes, and in-memory permit-store persistence are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, tool runtime invocation, shell/process execution, workspace-write execution, or external write by themselves. |
| Approval gate boundary | active / approval requirement evaluation only | No | `governance-internal-approval-gate` evaluates approval requirement status, reasons, and unresolved-state assertions only. `not_required`, resolved approvals, pending gate ids, protected branch signals, dirty workspace signals, and protected keyword signals are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, tool runtime invocation, shell/process execution, workspace-write execution, or external write by themselves. |
| Admission control boundary | active / admission status and requirement derivation only | No | `admission-control` evaluates admission status, risk signals, required capabilities, and required approvals only. `accepted`, `needs_approval`, `rejected`, capability matches, required approvals, external capabilities, and file write capabilities are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, tool runtime invocation, shell/process execution, workspace-write execution, or external write by themselves. |
| Delegation policy boundary | active / delegation level, approval requirement, and recovery action filtering only | No | `delegation-policy` records delegation history, calculates trust levels, derives approval requirements, manages proposals, filters recovery action names, and may persist delegation history. `full_delegation`, `requiresApproval(false)`, approved/applied proposals, filtered recovery actions, recovery action lists, historical trust, recorded resumes, and delegation file-store persistence are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, tool runtime invocation, shell/process execution, workspace-write execution, external write, or recovery execution by themselves. |
| Provider registry boundary | active / catalog selection, attestation, and manifest store only | No | `provider-registry` records provider manifests, attestations, selection summaries, optional provider object registrations, and provider manifest store state. Selected providers, provider-grant selection, routing-decision selection, registered executor/tool/remote-agent provider objects, remote-agent auth schemes, and manifest-store persistence are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, tool runtime invocation, shell/process execution, workspace-write execution, or external write by themselves. |
| Controlled provider execution dispatch preflight boundary | active / controlled_readonly_and_workspace_write_dispatch_preflight_matrix_only | No | The dispatch preflight matrix records the pre-runner stop/go contract for controlled read-only provider execution candidates and controlled workspace-write candidates. It may permit a request to reach the provider execution runner boundary only after exact provider id, side effect class, sandbox, approval policy, permit, plan hash, registry, preflight artifact, operation manifest where applicable, real-execution or workspace-write guard conditions, and governance stop checks pass. It is not general provider execute authorization, real Codex CLI authorization, general workspace-write authorization, host executor authorization, sub-agent runtime authorization, shell/process authorization, external-write authorization, release authorization, or evidence refresh authorization. |
| Controlled provider execution dispatcher boundary | active / controlled_readonly_and_workspace_write_pre_runner_dispatcher | No direct execute | The dispatcher is the pre-runner handoff layer for controlled read-only provider execution and controlled workspace-write local execution. It consumes dispatch preflight artifacts for read-only candidates, prepares controlled workspace-write dispatch input by recording a bound preflight artifact, and consumes workspace-write preflight artifacts for workspace-write candidates, then blocks before runner invocation when provider id, side effect class, sandbox, approval policy, provider execution plan hash, operation manifest hash, registry selection, executor plan, permit validation, environment preflight artifact binding, or governance stop checks fail. It may hand controlled workspace-write to the local runner only with workspace-write permit v2, declared operations, exact authorization id, and artifact binding; it does not call `provider.execute` directly for workspace-write, does not spawn Codex CLI, does not authorize general workspace-write, host executor, sub-agent runtime, shell/process, external write, release, or evidence refresh, and keeps final execution authority with the provider execution runner boundary. |
| Provider execution runner boundary | active / controlled read-only provider execute gate and controlled workspace-write local runner | Yes, narrow | `governance-internal-provider-execution-runner` is the direct gate before `provider.execute` for controlled read-only execution and the orchestration gate for controlled workspace-write local execution. It permits only `codex-cli + read_only + read-only` controlled read-only execution to call `provider.execute` with permit, executor plan, preflight artifact binding, real execution guard, and governance state checks. Controlled workspace-write may run only through `runWorkspaceWriteExecution` with workspace-write permit v2, declared operations, exact authorization id, clean git state, patch guard, rollback evidence, and sanitized report artifacts; it does not call `provider.execute` or real Codex CLI. Dry-runs never invoke execute, strategy `step_back`/`simulate` and recovery-phase states block before provider hooks, and non-codex provider execute, default real Codex CLI, sub-agent runtime, host executor, shell/process, and external write remain blocked. |
| Agent OS MCP server manifest boundary | active / manifest only; no runtime | No by itself | `agent-os-server-manifest` declares Agent OS MCP tool names, schemas, side effects, required capabilities, approval-required flags, and audit policy. `runtimeImplemented: false`, tool manifests, requiredCapabilities, approvalRequired, local_write side effects, provider planning output schemas, approval permit output schemas, listed tools, and manifest exports are not live MCP server startup, MCP tool invocation, tool runtime authorization, capability grant, approval grant, provider execute, Codex CLI invocation, desktop primitive invocation, sub-agent runtime authorization, host executor dispatch, shell/process execution, network serving, workspace-write execution, external write, or public execution surface by themselves. |
| Protocol MCP provider skeleton boundary | active / protocol mapping and disabled provider skeleton only | No by itself | `protocol-mcp` maps MCP refs/descriptors to tool manifests, descriptor echoes, provider manifests, and disabled invocation plans. Server refs, commandRef, endpointRef, tool manifests, invocation plans, fake providers, allowedTools, unknown side-effect defaults, and provider skeleton exports are not live MCP server connection, shell/process execution, network call, tool runtime authorization, MCP invocation authorization, provider execute, Codex CLI invocation, desktop primitive invocation, sub-agent runtime authorization, host executor dispatch, workspace-write execution, external write, or public execution surface by themselves; invoke remains disabled. |
| Protocol A2A remote provider skeleton boundary | active / agent card, task, artifact mapping and disabled remote provider skeleton only | No by itself | `protocol-a2a` maps local agent manifests, tasks, runs, artifacts, statuses, auth references, and fake transport records into A2A skeleton objects. Endpoint refs, agent cards, task skeletons, artifact URIs, auth schemes, remote-agent provider manifests, disabled remote providers, fake transports, and fake transport submissions are not network calls, remote runtime authorization, remote execution authorization, artifact fetch authorization, capability grants, sub-agent runtime authorization, provider execute, Codex CLI invocation, desktop primitive invocation, host executor dispatch, shell/process execution, live network serving, workspace-write execution, external write, or public execution surface by themselves; remote providers remain disabled and anonymous remote invocation remains rejected. |
| Agent OS SDK boundary | active / SDK method calls to local MCP runtime only | No by itself | `agent-os-sdk` translates SDK methods and explicit `callTool` invocations into local MCP runtime tool calls, and may carry controlled workspace-write prepare or dispatch payloads through the local runtime when explicit dependencies and a dispatcher are configured. SDK calls, call options, granted capabilities, approved mutating tools, allow-local-mutation, preferred provider, local runtime calls, approval permit issue, approval permit consumption, and controlled workspace-write prepare / dispatch are not capability grants, tool runtime authorization, general workspace-write execution, Codex CLI invocation, provider execution authorization, provider execute, desktop primitive invocation, sub-agent runtime authorization, host executor dispatch, shell/process execution, network access, workspace-write through `provider.execute`, or external write by themselves; general workspace-write remains blocked and real provider execution remains uninvoked. |
| Agent OS CLI boundary | active / argv parsing to local MCP runtime only | No by itself | `agent-os-cli` parses argv into local MCP runtime tool calls, returns sanitized argv in the result, and may carry controlled workspace-write prepare or dispatch payloads through the async local runtime when explicit dependencies and a dispatcher are configured. CLI grant flags are not capability grants, approve-tool flags are not tool runtime authorization, allow-local-mutation is not workspace-write execution, preferred provider is not Codex CLI invocation, parsed commands are not provider execution authorization, and local runtime calls, approval permit issue and consumption are not provider execution. Controlled workspace-write prepare / dispatch is limited to the governed async dispatcher path; general workspace-write and workspace-write through `provider.execute` remain blocked. Sanitized argv must not expose raw secrets and the CLI wrapper does not spawn Codex CLI. |
| Agent OS app-server wrapper boundary | active / HTTP-like request routing to local MCP runtime only | No by itself | `agent-os-app-server` converts HTTP-like request objects into local MCP runtime tool calls and audited response envelopes, and may carry controlled workspace-write prepare or dispatch payloads through the async local runtime when explicit server-side dependencies and a dispatcher are configured. HTTP-like routes are not live network servers, request envelopes are not capability grants, client-supplied gate fields remain ignored, status codes are not host executor receipts, and approval permit issue and consumption are not provider execution. Controlled workspace-write prepare / dispatch is limited to the governed async dispatcher path; general workspace-write and workspace-write through `provider.execute` remain blocked. Live HTTP servers remain unimplemented, network access remains absent, and real provider execution remains uninvoked. |
| Agent OS public surfaces boundary | active / public surface to local MCP runtime only | No by itself | `agent-os-sdk`, `agent-os-cli`, and `agent-os-app-server` translate SDK calls, argv, and HTTP-like request objects into local MCP runtime tool calls, and may carry controlled workspace-write prepare or dispatch payloads through explicit async public surface methods when dependencies and a dispatcher are configured. SDK calls are not provider execution authorization, CLI grant and approve-tool flags are not provider, tool runtime, Codex CLI, or sub-agent runtime authorization, allow-local-mutation is not workspace-write execution, preferred provider is not Codex CLI invocation, app-server request envelopes are not capability grants, HTTP-like routes are not live network servers, and status codes are not host executor receipts. Controlled workspace-write prepare / dispatch is limited to the governed async dispatcher path; general workspace-write and workspace-write through `provider.execute` remain blocked. |
| Provider-core execution primitives | active / manifest / permit / plan primitives only | No | `provider-core` can describe provider kinds, side-effect classes, execution plans, tool invocation plans, remote-agent manifests, and workspace-write permits. Those contracts remain schema/validation/planning evidence only; remote-agent, tool, and workspace-write primitives are not runtime authorization and do not bypass the Codex provider, host executor, sub-agent runtime, or workspace-write gates. |
| Preflight boundary | active / pre-execution signal evaluation only | No | `preflight` evaluates auth availability, required/available tool names, workspace cleanliness, protected branch state, and memory overview health. `ok`, missing tool checks, auth availability, workspace cleanliness, protected branch checks, memory overview, memory health status, warnings, and blocking issues are not provider execute, Codex CLI invocation, sub-agent runtime authorization, host executor dispatch, tool runtime authorization, shell/process execution, workspace-write authorization, workspace-write execution, external write, or runtime authorization by themselves. |
| Tool invocation planner boundary | active / tool manifest and invocation plan only | No | `tool-registry` can register built-in, MCP, and remote tool manifests, and `tool-invocation-planner` can produce `blocked`, `waiting_approval`, or `planned` tool invocation plans. Planned tool invocation status, remote.agent.invoke, external-write manifests, approval permits, and local-write sandbox derivation are not runtime invocation, sub-agent runtime authorization, external-write authorization, tool execution, host executor dispatch, Codex CLI invocation, provider execute, shell/process execution, or workspace-write execution by themselves. |
| Desktop agent strategy boundary | active / agent assignment and ownership plan only | No | `desktop-agent-strategy` plans bounded read-only analyst assignments and owned-write worker assignments. Parallel plans, worker assignments, write mode, ownership targets, maxAgents, read-only analyst assignments, and strategy reasons are not provider execute, Codex CLI invocation, desktop primitive invocation, sub-agent runtime authorization, host executor dispatch, shell/process execution, workspace-write execution, external write, runtime invocation, sub-agent spawn authorization, workspace-write authorization, or execution gates by themselves. |
| Host dispatcher provider boundary | active / controlled read-only and controlled workspace-write provider dispatch | Yes, narrow | `host-dispatcher` may dispatch verified `codex-cli` runner/provider plans with `read_only` side effects and `read-only` sandbox through provider permits, and may delegate controlled workspace-write input preparation and dispatch to `governance-internal-controlled-provider-dispatcher`. Formal read-only dispatch requires registry and metadata; controlled workspace-write requires permit v2, preflight artifact binding, declared operations, exact authorization id, and the local runner. General provider execution, general workspace-write, workspace-write through `provider.execute`, default real Codex CLI, sub-agent runtime, host executor, and external write remain blocked. |
| Codex desktop bridge boundary | active / explicit injected desktop host bridge | No by default | `codex-desktop-bindings` can map desktop primitives onto an explicitly injected runtime or tool-style host object. Runtime tool invocation is not default-authorized; shell commands require resolver input and support governed structured-command policy, patch results store hashes instead of patch bodies, and bridge wiring does not authorize Codex CLI, provider execution, sub-agent runtime, or host executor invocation. |
| Codex desktop live host boundary | active / explicit current host runtime and memory bundle | No by itself | `codex-desktop-live-host` composes an explicitly provided current host object or tool-style hooks into memory operations, a Codex desktop bridge, and `DesktopHostClient`. It requires a current host object with all runtime and required memory methods before bundle creation; smoke helpers create bundles only after readiness and summarize evidence. It does not authorize Codex CLI, provider execution, sub-agent runtime, host executor, workspace-write, or external write by itself. |
| Codex memory MCP client boundary | active / explicit MCP HTTP memory transport only | No by itself | `codex-memory-mcp-client` adapts an explicitly configured MCP HTTP endpoint into `CodexMemoryAdapter` checkpoint storage and recall. Initialize, close, `record_memory`, `search_memory`, and `memory_overview` JSON-RPC calls are memory transport calls only; they are not provider execution, host executor authorization, default endpoint lookup, Codex CLI invocation, sub-agent runtime invocation, shell/process execution, workspace-write execution, or external write authorization by themselves. bearer tokens authorize the MCP connection, not runtime execution. |
| Codex memory host client boundary | active / explicit injected memory operations only | No by itself | `codex-memory-host-client` adapts explicitly injected `record_memory`, `search_memory`, and optional `memory_overview` operations into `CodexMemoryAdapter` checkpoint storage and recall. These memory operation calls, adapter checkpoint writes, and MCP tool-style adapters are not host executor authorization, default host lookup, Codex CLI invocation, provider execute, sub-agent runtime invocation, shell/process execution, workspace-write execution, or external write by themselves. |
| Desktop host client boundary | active / host client facade | No by itself | `desktop-host-client` delegates `run` and `resume` to the desktop live adapter, captures current operator action lifecycle for review, delegates operator action host executor work to recovery-control review or explicit injected dispatch, and may delegate controlled workspace-write provider plans to the host dispatcher. It requires a bridge or bridge bindings and does not authorize Codex CLI, general provider execution, workspace-write through `provider.execute`, general workspace-write, sub-agent runtime, shell/process, or external write by itself; default host executor lookup remains blocked. |
| Desktop live adapter dispatch boundary | active / route-separated host dispatch or desktop bridge | No by itself | `runDesktopTask()` and `resumeDesktopTask()` first require task-scoped governance state, then route ready `codex-cli` decisions through `dispatchToHost()`, route ready `codex-cli` + `local_write` decisions through controlled workspace-write dispatch when an explicit dispatch input is provided, or route desktop-routed decisions through explicit handlers/bridge. Codex-cli routes do not invoke desktop bridge handlers, controlled workspace-write dispatch does not call Codex CLI spawn or desktop primitives, desktop routes do not invoke Codex CLI, and blocked decisions do not execute. Controlled workspace-write dispatch remains limited to the governed dispatcher path; general workspace-write and workspace-write through `provider.execute` remain blocked. |
| Host client example boundary | active / example-only host facade | No by itself | `host-client-example` demonstrates host composition by wiring memory, checkpoints, telemetry, observation stores, and an example bridge into the desktop live adapter. Its default bridge returns simulated desktop primitive envelopes, including simulated shell and patch primitive results, but does not run real shell/process, write the workspace, dispatch a host executor, invoke Codex CLI, call providers, invoke sub-agent runtime, or perform external writes. |
| Target host embedding boundary | active / explicit target-host contract and starter only | No by default | `target-host-object-contract`, `target-host-layer-skeleton`, and `target-host-embedding-starter` expose placeholder inspection, readiness status, directive builder composition, and bundle creation only after a fully wired explicit host object. Placeholder methods, scaffold ready status, createBundle, and directive builders are not host executor authorization, default real host execution, Codex CLI invocation, provider execute, sub-agent runtime invocation, shell/process execution, workspace-write, or external write by themselves. |
| Workspace-write fake canary | guarded | No | Validates permit v2, patch guard, rollback evidence, and replay blocking without real host writes. |
| Workspace-write real canary | experimental / blocked by default | No by default | Requires [workspace-write release gate](WORKSPACE_WRITE_RELEASE_GATE.md), [workspace-write canary runbook](runbooks/WORKSPACE_WRITE_CANARY_RUNBOOK.md), a fresh explicit authorization packet for the named canary, permit v2 controls, fake-canary v2 validation, and rollback evidence. |
| General workspace write | blocked | No | A bounded canary does not promote this class. |
| General provider execution | blocked | No | Requires a separate gate and explicit authorization. |
| Codex App Server governance adapter | pre-production beta / normalized fake profile validated | No direct execution | Correlates thread/turn/item file-change proposals with approval requests, authorizes through shared capability facts, previews only in an independently cloned repository, journals before accept, and verifies App Server post-state. Unproven approval interception is observe-only. |
| Structured file-change auto approval | guarded / create-update only | App Server applies accepted changes | Requires exact target facts, clean non-protected branch, matching HEAD, named network/filesystem isolation enforcement, a hard policy allowlist, diff limits, and passing exact-argv checks. Delete, rename, command, permission, secret/env, network/external, and release/deploy are not configurable exceptions. |
| Governed retain and rollback | guarded / receipt-bound | Narrow operator-authorized rollback only | Retain verifies exact target hashes and no outside changes. Rollback requires a fresh single-use permit, matching HEAD/repository/after-hashes, and no drift; uncertainty enters reconciliation instead of guessing success. |
| External write | blocked | No | Includes comments, issues, remote service writes, database writes, publishing, and deployment. |
| Release, package publish, tag, deployment | blocked by default | No | Requires release-specific authorization and successful release gates. |
| Secret, credential, token, env mutation | blocked by default | No | Requires explicit named-secret authorization; never expose secret values. |

## Historical Runtime-Posture Markers

The default execution posture is local, inspectable, and non-executing:

1. Prefer dry-run or in-memory paths before host execution.
2. Inject bridges, stores, policy files, and host clients explicitly.
3. Normalize failures into stable error classes before governance records them.
4. Preserve evidence as refs, hashes, statuses, reason codes, and summaries.
5. Fail closed when a required gate, permit, evidence binding, or state-sync
   claim cannot be verified.

## Historical Operating-Entry Markers

Use these first:

| Need | Entry point |
| --- | --- |
| Current repository state | `docs/current/CURRENT_STATE.md` |
| Current Codex governance architecture | `CODEX_EXECUTION_GOVERNANCE_ARCHITECTURE.md` |
| Codex App Server file-change procedure | `runbooks/CODEX_APP_SERVER_FILE_CHANGE_GOVERNANCE.md` |
| Current governance capability status | This document |
| PR/release validation policy | `RELEASE_GATE_MATRIX.md` |
| Evidence safety boundary | `EVIDENCE_POLICY.md` |
| Threat and control map | `THREAT_MODEL.md` |
| Change impact rules | `CHANGE_CONTROL.md` |
| Workspace-write promotion/block rules | `WORKSPACE_WRITE_RELEASE_GATE.md` |
| Documentation structure check scope | `DOCS_AUTOMATION_SPEC.md` |
| Term definitions | `GLOSSARY.md` |
| Read-only controlled execution procedure | `runbooks/READONLY_CONTROLLED_EXECUTION_RUNBOOK.md` |
| Workspace-write canary procedure | `runbooks/WORKSPACE_WRITE_CANARY_RUNBOOK.md` |
| Phase 6 runtime hardening baseline | `PHASE_6_CONTROLLED_EXECUTION_RUNTIME_HARDENING_BASELINE.md` |
| Phase 6 runtime hardening closeout | `PHASE_6_CONTROLLED_EXECUTION_RUNTIME_HARDENING_CLOSEOUT.md` |
| Controlled read-only execution evidence binding | `PR_23C_EXECUTION_EVIDENCE_BINDING.md` |
| Read-only provider permit lifecycle hardening | `PHASE_6_READONLY_PROVIDER_PERMIT_LIFECYCLE_HARDENING.md` |
| Workspace-write permit v2 schema and validators | `PR_23D_WORKSPACE_WRITE_PERMIT_V2.md` |
| Workspace-write fake canary v2 | `PR_23E_WORKSPACE_WRITE_FAKE_CANARY_V2.md` |
| Runtime operator actionability closeout | `PHASE_7_RUNTIME_OPERATOR_ACTIONABILITY_CLOSEOUT.md` |
| Runtime operator action lifecycle closeout | `PHASE_8_OPERATOR_ACTION_LIFECYCLE_CLOSEOUT.md` |
| Runtime operator action host lifecycle closeout | `PHASE_9_OPERATOR_ACTION_HOST_LIFECYCLE_CLOSEOUT.md` |
| Runtime operator action executor gate closeout | `PHASE_10_OPERATOR_ACTION_EXECUTOR_GATE_CLOSEOUT.md` |
| Runtime operator action host executor boundary taskbook | `PHASE_11_OPERATOR_ACTION_HOST_EXECUTOR_BOUNDARY_TASKBOOK.md` |
| Runtime operator action host executor boundary closeout | `PHASE_11_OPERATOR_ACTION_HOST_EXECUTOR_BOUNDARY_CLOSEOUT.md` |
| Runtime operator action host client review surface closeout | `PHASE_12_OPERATOR_ACTION_HOST_CLIENT_REVIEW_SURFACE_CLOSEOUT.md` |
| Runtime operator action host executor dispatch taskbook | `PHASE_13_OPERATOR_ACTION_HOST_EXECUTOR_DISPATCH_TASKBOOK.md` |
| Runtime operator action host executor dispatch closeout | `PHASE_13_OPERATOR_ACTION_HOST_EXECUTOR_DISPATCH_CLOSEOUT.md` |
| Agent executor receipt contract | `PHASE_14_AGENT_EXECUTOR_RECEIPT_CONTRACT.md` |
| Agent executor adapter authorization taskbook | `PHASE_15_AGENT_EXECUTOR_ADAPTER_AUTHORIZATION_TASKBOOK.md` |
| Agent executor adapter review-only closeout | `PHASE_15_AGENT_EXECUTOR_ADAPTER_REVIEW_ONLY_CLOSEOUT.md` |
| Agent executor adapter sandbox contract closeout | `PHASE_15_AGENT_EXECUTOR_ADAPTER_SANDBOX_CONTRACT_CLOSEOUT.md` |
| Agent executor adapter dispatch authorization taskbook | `PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_AUTHORIZATION_TASKBOOK.md` |
| Agent executor adapter dispatch authorization review-only closeout | `PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_AUTHORIZATION_REVIEW_ONLY_CLOSEOUT.md` |
| Agent executor adapter dispatch sandbox dry-run taskbook | `PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_SANDBOX_DRY_RUN_TASKBOOK.md` |
| Agent executor adapter dispatch sandbox dry-run closeout | `PHASE_16_AGENT_EXECUTOR_ADAPTER_DISPATCH_SANDBOX_DRY_RUN_CLOSEOUT.md` |
| Agent task control dispatch boundary taskbook | `PHASE_17_AGENT_TASK_CONTROL_DISPATCH_BOUNDARY_TASKBOOK.md` |
| Agent task control dispatch authorization review-only closeout | `PHASE_17_AGENT_TASK_CONTROL_DISPATCH_AUTHORIZATION_REVIEW_ONLY_CLOSEOUT.md` |
| Agent task control dispatch sandbox dry-run taskbook | `PHASE_18_AGENT_TASK_CONTROL_DISPATCH_SANDBOX_DRY_RUN_TASKBOOK.md` |
| Agent task control dispatch sandbox dry-run closeout | `PHASE_18_AGENT_TASK_CONTROL_DISPATCH_SANDBOX_DRY_RUN_CLOSEOUT.md` |
| Controlled read-only provider execution acceptance | `npm run governance -- acceptance controlled-readonly-provider-execution --check` for no-write review; omit `--check` to refresh evidence intentionally. |
| Available current checks | `npm run governance -- list` |
| Governance docs structure check | `npm run docs:governance` |
| Archived checks | `npm run governance -- list --all` |


### Historical Control-Plane Audit Command Markers

These commands are retained for fail-closed boundary-audit compatibility;
they are not active roadmap entries or execution authorization.

```bash
npm run governance -- audit codex-cli-host-boundary
npm run governance -- audit agent-os-local-runtime-boundary
npm run governance -- audit state-sync-boundary
npm run governance -- audit execution-eligibility-boundary
npm run governance -- audit execution-planner-boundary
npm run governance -- audit desktop-decision-runner-boundary
npm run governance -- audit final-host-locator-boundary
```
